import React from 'react'
import Layout from '../components/Layout'
import Roadmap from '../components/Roadmap'

export default () => {
    return (
        <Layout>
            <Roadmap />
        </ Layout>
    );
}